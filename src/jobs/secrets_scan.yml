description: >
  Scan the repository for leaked secrets using gitleaks.
executor: base
steps:
  - checkout
  - install_gitleaks
  - run_gitleaks
  - store_artifacts:
      path: /tmp/gitleaks-report.json
      destination: gitleaks-report.json
