description: >
  Example showing how to use Munitor with a Java webapp repo.
  Each repo needs two files: .circleci/config.yml and .faber.yml.

usage:
  version: 2.1
  setup: true
  orbs:
    munitor: KofTwentyTwo/munitor@1
  workflows:
    setup:
      jobs:
        - munitor/generate_pipeline
