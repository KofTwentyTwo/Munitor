description: >
  Generate a Dockerfile based on the pipeline type in .munitor.yml.
steps:
  - run:
      name: Generate Dockerfile
      environment:
        MUNITOR_SCRIPT_DIR: /tmp/munitor
      command: << include(scripts/packed_generate_dockerfile.sh) >>
