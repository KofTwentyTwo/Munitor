description: >
  Run Maven tests and store JUnit XML results.
steps:
  - run:
      name: Maven test
      command: << include(scripts/mvn_test.sh) >>
  - store_test_results:
      path: target/surefire-reports
  - store_artifacts:
      path: target/surefire-reports
      destination: test-results
